public with sharing class Account_trigger2 {
    /*
    trigger AccountTrigger2 on Account (after update) {
        if(trigger.isAfter && trigger.isUpdate){
            Account_Trigger_Handler2.updateCase(Trigger.newMap,Trigger.oldMap);
        }
    }
    */
}